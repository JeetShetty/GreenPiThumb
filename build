# Run unit tests and calculate code coverage.
coverage run \
  --source greenpithumb \
  --omit "greenpithumb/dht11/*" \
  -m unittest discover &&
# Check that source has correct formatting.
yapf --diff --recursive --style google ./ --exclude="./greenpithumb/dht11/*"
